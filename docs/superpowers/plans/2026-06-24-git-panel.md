# Painel Git — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar um painel Git ao OKF Studio para ver mudanças, commitar (tudo) e fazer push da biblioteca aberta, com "git init" para pasta sem repositório.

**Architecture:** Um módulo `git.js` no processo principal roda o `git` (CLI) via `child_process` com `cwd` = raiz da biblioteca (`currentRoot`), exposto por IPC `git:*`. O renderer mostra uma tela de overlay (padrão do Grafo/Validar/Manual) com status, mensagem de commit e botões; o status se atualiza ao vivo junto com o observador de arquivos.

**Tech Stack:** Electron 42, git CLI (`child_process.execFile`), padrões de overlay/IPC já existentes no app.

---

## Visão geral de arquivos

| Arquivo | Responsabilidade | Ação |
|---|---|---|
| `git.js` | Rodar git no `currentRoot`; handlers IPC `git:*` | Criar |
| `test-git.js` | Smoke: status/commit num repo temporário | Criar |
| `main.js` | Registrar os handlers git | Modificar |
| `preload.js` | Ponte `okf.git.*` | Modificar |
| `renderer/index.html` | Botão ⎇ Git + tela `#git-view` | Modificar |
| `renderer/styles.css` | Estilos do painel | Modificar |
| `renderer/renderer.js` | showGit/renderGit/refresh/commit/push/init; refresh ao vivo | Modificar |
| `test-renderer.js` | Smoke: ponte `okf.git` | Modificar |

---

## Task 1: Backend Git (`git.js`) + IPC + ponte

**Files:**
- Create: `git.js`, `test-git.js`
- Modify: `main.js`, `preload.js`, `package.json` (script `test:git`)

- [ ] **Step 1: Escrever o smoke test (falha primeiro)**

Create `test-git.js`:
```js
'use strict';
// Smoke: status lista um arquivo novo; após commit, fica limpo. Run: npm run test:git
const fs = require('fs');
const os = require('os');
const path = require('path');
const { execFileSync } = require('child_process');
const { init, status, commit } = require('./git.js');

(async () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'okf-git-'));
  await init(dir);
  execFileSync('git', ['config', 'user.email', 't@t.dev'], { cwd: dir });
  execFileSync('git', ['config', 'user.name', 'Test'], { cwd: dir });
  fs.writeFileSync(path.join(dir, 'a.md'), '# a');

  const s1 = await status(dir);
  const listed = s1.ok && s1.repo && s1.files.length === 1 && s1.files[0].path === 'a.md';

  const c = await commit(dir, 'add a');
  const s2 = await status(dir);
  const clean = c.ok && s2.ok && s2.files.length === 0;

  console.log('lista 1 arquivo:', listed, '| limpo após commit:', clean);
  console.log(listed && clean ? 'RESULT: PASS' : 'RESULT: FAIL');
  process.exit(listed && clean ? 0 : 1);
})();
```

Add to `package.json` `scripts`:
```json
    "test:git": "node test-git.js",
```

- [ ] **Step 2: Rodar e confirmar a falha**

Run: `npm run test:git`
Expected: FAIL — `Cannot find module './git.js'`.

- [ ] **Step 3: Implementar `git.js`**

Create `git.js`:
```js
'use strict';
// Executa o git (CLI) com cwd = raiz da biblioteca. Sem manuseio de credenciais.
const { execFile } = require('child_process');

function run(cwd, args) {
  return new Promise((resolve) => {
    execFile('git', args, { cwd, windowsHide: true, maxBuffer: 10 * 1024 * 1024 }, (err, stdout, stderr) => {
      if (err) resolve({ ok: false, error: String(stderr || err.message || '').trim(), stdout: stdout || '' });
      else resolve({ ok: true, stdout: stdout || '' });
    });
  });
}

async function isRepo(cwd) {
  const r = await run(cwd, ['rev-parse', '--is-inside-work-tree']);
  return r.ok && r.stdout.trim() === 'true';
}

async function status(cwd) {
  if (!(await isRepo(cwd))) return { ok: true, repo: false, files: [], branch: '', hasRemote: false };
  const st = await run(cwd, ['status', '--porcelain']);
  if (!st.ok) return { ok: false, error: st.error };
  const files = st.stdout.split(/\r?\n/).filter(Boolean).map((line) => ({
    code: line.slice(0, 2).trim() || line.slice(0, 2),
    path: line.slice(3)
  }));
  const br = await run(cwd, ['rev-parse', '--abbrev-ref', 'HEAD']);
  const rm = await run(cwd, ['remote']);
  return { ok: true, repo: true, files, branch: br.ok ? br.stdout.trim() : '', hasRemote: !!(rm.ok && rm.stdout.trim()) };
}

async function commit(cwd, message) {
  const add = await run(cwd, ['add', '-A']);
  if (!add.ok) return add;
  return run(cwd, ['commit', '-m', message]);
}

function push(cwd) { return run(cwd, ['push']); }
function init(cwd) { return run(cwd, ['init']); }

function registerGitHandlers(ipcMain, getRoot) {
  const guard = (fn) => () => { const r = getRoot(); return r ? fn(r) : { ok: false, error: 'Abra uma biblioteca primeiro.' }; };
  ipcMain.handle('git:status', guard(status));
  ipcMain.handle('git:push', guard(push));
  ipcMain.handle('git:init', guard(init));
  ipcMain.handle('git:commit', (_e, msg) => { const r = getRoot(); return r ? commit(r, msg) : { ok: false, error: 'Abra uma biblioteca primeiro.' }; });
}

module.exports = { isRepo, status, commit, push, init, registerGitHandlers };
```

- [ ] **Step 4: Rodar e confirmar PASS**

Run: `npm run test:git`
Expected: `lista 1 arquivo: true | limpo após commit: true` e `RESULT: PASS`.

- [ ] **Step 5: Ligar no `main.js`**

Em `main.js`, no topo (após o require do watcher):
```js
const { registerGitHandlers } = require('./git.js');
```
Junto às outras chamadas de registro de IPC (por exemplo, perto de `registerPtyHandlers` — se não existir, logo antes de `app.whenReady`), adicionar:
```js
registerGitHandlers(ipcMain, () => currentRoot);
```
(`currentRoot` já existe no `main.js`, definido em `bundle:read`/`bundle:sample`.)

- [ ] **Step 6: Expor a ponte no `preload.js`**

Em `preload.js`, dentro do objeto exposto em `window.okf` (após `onBundleChanged`), adicionar:
```js
  git: {
    status: () => ipcRenderer.invoke('git:status'),
    commit: (message) => ipcRenderer.invoke('git:commit', message),
    push: () => ipcRenderer.invoke('git:push'),
    init: () => ipcRenderer.invoke('git:init')
  },
```

- [ ] **Step 7: Commit**
```bash
git add git.js test-git.js main.js preload.js package.json
git commit -m "feat: backend Git (status/commit/push/init) com IPC e ponte"
```

---

## Task 2: Painel Git (UI)

**Files:**
- Modify: `renderer/index.html` (botão + `#git-view`)
- Modify: `renderer/styles.css`
- Modify: `renderer/renderer.js`

- [ ] **Step 1: Botão e tela (HTML)**

Em `renderer/index.html`, dentro de `<div class="tools">`, logo após `<button id="btn-claude" ...>`, adicionar:
```html
      <button id="btn-git" title="Git: ver mudanças, commit e push" disabled>⎇ Git</button>
```
Em `renderer/index.html`, logo após o bloco `<div id="manual-view" ...>...</div>` (e antes de `</section>`), adicionar:
```html
      <!-- Git -->
      <div id="git-view" class="hidden">
        <div class="graph-head">
          <strong>Git</strong>
          <span id="git-branch" class="muted"></span>
          <span class="grow"></span>
          <button id="git-refresh">Atualizar</button>
          <button id="git-close">Fechar</button>
        </div>
        <div class="git-body">
          <div id="git-norepo" class="git-norepo hidden">
            <p>Esta pasta não é um repositório git.</p>
            <button id="git-init" class="primary">Inicializar (git init)</button>
          </div>
          <div id="git-list" class="git-list"></div>
          <div id="git-commit" class="git-commit">
            <input id="git-msg" type="text" placeholder="Mensagem do commit…" />
            <button id="git-do-commit" class="primary">Commit</button>
            <button id="git-do-push">Push</button>
          </div>
        </div>
      </div>
```

- [ ] **Step 2: Estilos (CSS)**

Em `renderer/styles.css`, ao final, adicionar:
```css
/* git panel */
.git-body{flex:1;min-height:0;display:flex;flex-direction:column;padding:14px 22px;
  max-width:900px;margin:0 auto;width:100%}
.git-norepo{text-align:center;color:var(--muted);padding:20px}
.git-norepo p{margin-bottom:12px}
.git-list{flex:1;min-height:0;overflow:auto;border:1px solid var(--line);border-radius:8px;margin-bottom:12px}
.git-empty{padding:14px;color:var(--muted)}
.git-item{display:flex;align-items:center;gap:10px;padding:6px 12px;border-bottom:1px solid var(--line)}
.git-item:last-child{border-bottom:none}
.git-code{font-family:Consolas,monospace;font-size:11px;font-weight:600;min-width:24px;text-align:center;
  border-radius:4px;padding:1px 4px}
.git-code.mod{color:var(--warn)} .git-code.add{color:var(--good)} .git-code.del{color:var(--bad)}
.git-path{font-size:13px}
.git-commit{display:flex;gap:8px}
#git-msg{flex:1}
```

- [ ] **Step 3: Lógica do painel (renderer.js)**

Em `renderer/renderer.js`, adicionar (perto de `showValidation`/`showManual`):
```js
/* ---------- Painel Git ---------- */
function showGit() {
  if (!state.root) return;
  closeOverlays();
  $('git-view').classList.remove('hidden');
  refreshGit();
}
async function refreshGit() {
  let s;
  try { s = await window.okf.git.status(); } catch (e) { s = { ok: false, error: String(e) }; }
  renderGit(s);
}
function renderGit(s) {
  const branch = $('git-branch'), list = $('git-list'), commit = $('git-commit'), norepo = $('git-norepo');
  if (!s || !s.ok) {
    norepo.classList.add('hidden'); commit.classList.add('hidden'); branch.textContent = '';
    list.innerHTML = `<div class="git-empty">Erro: ${escapeHtml((s && s.error) || '')}</div>`;
    return;
  }
  if (!s.repo) {
    norepo.classList.remove('hidden'); commit.classList.add('hidden');
    list.innerHTML = ''; branch.textContent = '';
    return;
  }
  norepo.classList.add('hidden'); commit.classList.remove('hidden');
  branch.textContent = s.branch ? ('branch: ' + s.branch) : '';
  $('git-do-push').disabled = !s.hasRemote;
  $('git-do-push').title = s.hasRemote ? 'Enviar (push)' : 'Sem remoto — adicione pelo terminal (git remote add origin …)';
  if (!s.files.length) { list.innerHTML = '<div class="git-empty">Nada para commitar — tudo limpo.</div>'; return; }
  list.innerHTML = s.files.map((f) => {
    const cls = /D/.test(f.code) ? 'del' : (/[AR?]/.test(f.code) ? 'add' : 'mod');
    return `<div class="git-item"><span class="git-code ${cls}">${escapeHtml(f.code || '?')}</span>` +
           `<span class="git-path">${escapeHtml(f.path)}</span></div>`;
  }).join('');
}
async function gitCommit() {
  const msg = $('git-msg').value.trim();
  if (!msg) { toast('Informe a mensagem do commit.', 'bad'); return; }
  const r = await window.okf.git.commit(msg);
  if (r && r.ok) { toast('Commit feito.', 'good'); $('git-msg').value = ''; refreshGit(); }
  else toast('Erro no commit: ' + ((r && r.error) || ''), 'bad');
}
async function gitPush() {
  const r = await window.okf.git.push();
  if (r && r.ok) toast('Push concluído.', 'good');
  else toast('Erro no push: ' + ((r && r.error) || '') + ' — faça login pelo terminal se necessário.', 'bad');
}
async function gitInit() {
  const r = await window.okf.git.init();
  if (r && r.ok) { toast('Repositório inicializado.', 'good'); refreshGit(); }
  else toast('Erro: ' + ((r && r.error) || ''), 'bad');
}
```

Em `renderer/renderer.js`, na função `closeOverlays`, **incluir** o git-view:
```js
function closeOverlays(){ $('graph-view').classList.add('hidden'); $('validate-view').classList.add('hidden'); $('manual-view').classList.add('hidden'); $('git-view').classList.add('hidden'); }
```

Em `renderer/renderer.js`, na função `loadBundle`, na lista que habilita botões, **incluir** `'btn-git'`:
```js
  ['btn-reload','btn-new','btn-graph','btn-validate','btn-claude','btn-git','search','type-filter'].forEach(id => $(id).disabled = false);
```

Em `renderer/renderer.js`, dentro de `init()`, adicionar as ligações:
```js
  $('btn-git').onclick = showGit;
  $('git-close').onclick = () => { closeOverlays(); if (state.current) showViewer(); };
  $('git-refresh').onclick = refreshGit;
  $('git-init').onclick = gitInit;
  $('git-do-commit').onclick = gitCommit;
  $('git-do-push').onclick = gitPush;
```

Em `renderer/renderer.js`, **substituir** a linha que liga o watcher (hoje `window.okf.onBundleChanged(reloadFromDisk);`) por uma que também atualiza o painel Git quando aberto:
```js
  window.okf.onBundleChanged(() => { reloadFromDisk(); if (!$('git-view').classList.contains('hidden')) refreshGit(); });
```

Em `renderer/renderer.js`, no listener de `keydown` (Escape), **incluir** o git-view na condição que fecha overlays. Localizar a verificação dos overlays e adicionar `|| $('git-view').classList.contains('hidden')===false`:
```js
    if (e.key === 'Escape') { closeModal(); closeConceptPicker(); if ($('graph-view').classList.contains('hidden')===false || $('validate-view').classList.contains('hidden')===false || $('manual-view').classList.contains('hidden')===false || $('git-view').classList.contains('hidden')===false){ closeOverlays(); if(state.current) showViewer(); } }
```

- [ ] **Step 4: Verificar manualmente**

Run: `npm start`, abra **✨ Exemplo** (a biblioteca de exemplo está dentro deste repo git) → clique em **⎇ Git**:
- A lista mostra as mudanças (se houver) e a branch atual.
- Digite uma mensagem e clique **Commit** → a lista limpa.
- **Push** fica desabilitado se não houver remoto.
- Abra uma pasta **sem git** → o painel oferece **Inicializar (git init)**.
Feche o app.

- [ ] **Step 5: Commit**
```bash
git add renderer/index.html renderer/styles.css renderer/renderer.js
git commit -m "feat: painel Git (status, commit, push, init) com refresh ao vivo"
```

---

## Task 3: Smoke da ponte e README

**Files:**
- Modify: `test-renderer.js` (ponte `okf.git`)
- Modify: `README.md`

- [ ] **Step 1: Smoke da ponte (test-renderer.js)**

Em `test-renderer.js`, no objeto retornado pelo `executeJavaScript`, adicionar:
```js
    gitBridge: !!(window.okf && window.okf.git && typeof window.okf.git.status === 'function' &&
      typeof window.okf.git.commit === 'function'),
```
Na linha que computa `okBridge`, **incluir** `&& result.gitBridge === true`:
```js
  const okBridge = result.okfBridge === 'object' && result.updateBridge === true && result.claudeBridge === true && result.gitBridge === true;
```

- [ ] **Step 2: Rodar a suíte**

Run: `npm test && npm run test:git && npm run test:watch && npm run test:ui`
Expected: `TESTE OK`; `RESULT: PASS` (git); `RESULT: PASS` (watch); `RESULT: PASS` (ui) com `CSP violations: none`.

- [ ] **Step 3: Atualizar o README**

Em `README.md`, na lista de recursos ("## O que o app faz"), após a linha do "Claude Code + atualização ao vivo", adicionar:
```markdown
- **Painel Git** (botão ⎇ Git): vê os arquivos alterados, faz **commit** e
  **push** da biblioteca sem sair do app; oferece **git init** se a pasta ainda
  não for um repositório. O status atualiza ao vivo conforme os arquivos mudam.
```
Em `README.md`, na seção "## Estrutura do projeto", após a linha do `watcher.js`, adicionar:
```markdown
├── git.js               Operações git (status/commit/push/init) — processo principal
```

- [ ] **Step 4: Commit + push**
```bash
git add test-renderer.js README.md
git commit -m "test+docs: smoke da ponte Git e README do painel"
git push origin HEAD
```

---

## Notas de verificação

- O `git.js` depende do **git no PATH** (já é o caso). Os erros do git são
  repassados ao usuário via toast (ex.: push sem upstream/credencial).
- O parsing do `git status --porcelain` usa os 2 primeiros caracteres como
  código de estado e o restante (após a posição 3) como caminho — suficiente
  para exibição (M/A/D/??).
- `currentRoot` é definido no `main.js` em `bundle:read`/`bundle:sample`; o painel
  opera sempre na biblioteca aberta.
```
